# Path in repo: SMS/smsApp/tests.py
from django.test import TestCase  # noqa: F401

# First tests land in Phase 1B alongside the custom User model
# (per the master spec's testing requirements: models, permissions,
# and auth all need coverage, not just "it works manually").
