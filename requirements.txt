# =============================================================================
# SMS + LMS Platform — Phase 1 Requirements
# Path in repo: SMS/requirements.txt  (same level as manage.py)
# =============================================================================

# --- Core framework -----------------------------------------------------
Django==5.0.6

# --- Database -------------------------------------------------------------
psycopg2-binary==2.9.9        # PostgreSQL driver (works against Supabase Postgres)

# --- Environment / configuration ------------------------------------------
python-decouple==3.8          # Clean separation of settings from code (.env support)
dj-database-url==2.2.0        # Parse DATABASE_URL env var into Django DB config

# --- Static files (production-ready from day 1) ----------------------------
whitenoise==6.6.0

# --- Dev quality of life ----------------------------------------------------
django-extensions==3.2.3

# =============================================================================
# Deferred to later phases (do NOT install yet — listed here for visibility):
#   djangorestframework        -> Phase 2 (API layer)
#   djangorestframework-simplejwt -> Phase 2 (JWT auth for Flutter)
#   django-cors-headers         -> Phase 2 (API layer)
#   drf-spectacular             -> Phase 2 (OpenAPI/Swagger docs)
#   celery, redis                -> background-processing phase
#   supabase / storage3           -> Supabase Storage integration phase
#   weasyprint, reportlab          -> report/transcript PDF generation phase
# =============================================================================
